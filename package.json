{
  "name": "computable-records",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A typed, computed, event-sourced database — Notion's database half with a spreadsheet's brain.",
  "license": "MIT",
  "author": "rossjdharrison",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "bundle": "node scripts/bundle-model.mjs",
    "gate": "node scripts/gate.mjs",
    "test": "node --test \"packages/{values,formula,events,query,ontology,client}/src/**/*.test.ts\" \"packages/core-runtime/test/**/*.test.ts\" \"packages/core-wasm/test/**/*.test.ts\"",
    "pretest": "npm run bundle",
    "test:server": "npm -w @app/server run test",
    "typecheck": "tsc -p packages/values/tsconfig.json && tsc -p packages/formula/tsconfig.json && tsc -p packages/events/tsconfig.json && tsc -p packages/query/tsconfig.json && tsc -p packages/ontology/tsconfig.json && tsc -p packages/server/tsconfig.json && tsc -p packages/client/tsconfig.json && tsc -p packages/core-runtime/tsconfig.json && tsc -p packages/core-wasm/tsconfig.json",
    "pretypecheck": "npm run bundle",
    "check": "npm run gate && npm run typecheck && npm run test && npm run test:server"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.8.3"
  }
}
