// @app/core-runtime — the sealed, extern-controlled core + its mock externs.
export { createCore } from './core.ts';
export { mockExterns } from './mock-externs.ts';
export type { Cassette, Collection, Core, Externs, Prop, RowOp, RowState } from './types.ts';
