{
  "name": "@app/server",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "The CollectionDO — one Durable Object per collection: seq-ordered op log, embedded SQLite, computed-column recompute, and WebSocket fanout.",
  "dependencies": {
    "@core/values": "*",
    "@core/events": "*",
    "@core/query": "*",
    "@core/formula": "*",
    "@core/ontology": "*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250906.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.33.0"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "dev": "wrangler dev"
  }
}
