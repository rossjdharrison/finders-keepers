{
  "name": "@app/client",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "The browser hosts — the client-only cassette player (catalogue/play/loom, running the sealed @core in QuickJS wasm in-tab) and the DB view engine (data-defined views over the WorkspaceDO WebSocket). One renderer registry (table/board/docs/self-portrait/journey); Vite + @preact/signals-core, no framework.",
  "dependencies": {
    "@core/values": "*",
    "@core/query": "*",
    "@core/ontology": "*",
    "@app/core-runtime": "*",
    "@app/core-wasm": "*",
    "@preact/signals-core": "^1.8.0",
    "quickjs-emscripten-core": "^0.32.0",
    "@jitl/quickjs-wasmfile-release-sync": "^0.32.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.1.0"
  },
  "scripts": {
    "predev": "node ../../scripts/bundle-model.mjs",
    "dev": "vite",
    "prebuild": "node ../../scripts/bundle-model.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=rowblaa-autoverzekering --branch=main",
    "test": "node --test \"src/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json"
  }
}
